<!DOCTYPE html>
<html lang="en">
<head>
	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous">
	<title>Dojo Detail</title>
</head>
<body>
	<div style="width: 70%; margin: 10% auto; border: thin solid gray; border-radius: 5px; padding: 3%;">
		<h1><%= @dojo.branch %> Dojo</h1>
		<hr>
		<h3>Adress: <%= @dojo.street %></h3>
		<h3>City: <%= @dojo.city %> State: <%= @dojo.state %></h3>
		<!-- <h3>State: <%= @dojo.state %></h3> -->
		<hr><h3>Cohort</h3>
	<div class="table-responsive">
		<table class="table table-striped" >
			<thead class="thead-dark">
				<th scope="col">Frist Name</th>
				<th scope="col">Last Name</th>
				<th scope="col">Email</th>
				<th scope="col">Actions</th>
			</thead>
			<tbody>
				<% @students.each do |student| %>
					<tr>
						<th scope="row"><%= student.first_name %></th>
						<td><%= student.last_name %></td>
						<td><%= student.email %></td>
						<td><a href="/dojos/<%= student.dojo_id %>/students/<%= student.id %>">Show</a> | 
							<a href="/dojos/<%= student.dojo_id %>/students/<%= student.id %>/edit">Edit</a> |
							<a rel="nofollow" data-method="delete" href="/dojos/<%= student.dojo_id %>/students/<%= student.id %>">Destroy</a>
						</td>
					</tr>
				<% end %>
			</tbody>
		</table>
	<div>
		<p class="text-center"><a href="/dojos">Home</a> | 
		<a href="/dojos/<%= @dojo.id %>/students/new">Add Student</a></p>
	</div>
	</div>
</body>
</html>